# 台灯控制程序 Agent 使用说明

本文档面向 AI 智能体（Agent）。描述如何在 macOS 终端中通过命令控制小米台灯2，包括调用方式、参数规则、输出格式、错误判断和使用限制。

---

## 1. 调用方式

### 首选方式

```
lamp <命令> [参数]
```

`lamp` 是一个 Go 编译的独立二进制文件，位于 `~/bin/lamp`。`~/bin` 已加入 `~/.zshenv`，**任何 shell 环境（包括非交互式）均可直接使用**，无需激活虚拟环境，无需切换目录。

### 备用方式（PATH 不可用时，使用完整路径）

```
/Users/huangke/bin/lamp <命令> [参数]
```

---

## 2. 支持的命令

### 2.1 开灯

```
lamp 开
```

成功输出：
```
台灯已开
```

---

### 2.2 关灯

```
lamp 关
```

成功输出：
```
台灯已关
```

---

### 2.3 查询状态

```
lamp 状态
```

成功输出（示例）：
```
状态：开
亮度：80%
色温：4000K
```

- `状态` 的值为 `开` 或 `关`
- `亮度` 范围 1–100，单位 %
- `色温` 范围 2700–5100，单位 K

---

### 2.4 设置亮度

```
lamp 亮度 <数值>
```

- 参数：整数，范围 1–100
- 超出范围会被自动截断（1 以下取 1，100 以上取 100）

示例：
```
lamp 亮度 80
```

成功输出（实际生效值）：
```
亮度已设置：80%
```

---

### 2.5 设置色温

```
lamp 色温 <数值>
```

- 参数：整数，范围 2700–5100（单位 K，数值越大越冷白）
- 超出范围会被自动截断

示例：
```
lamp 色温 4000
```

成功输出（实际生效值）：
```
色温已设置：4000K
```

---

### 2.6 场景模式

```
lamp <场景名> [亮度]
```

可用场景名（共 5 种）：

| 场景名 | 色温 | 适用场合 |
|--------|------|----------|
| `暖白` | 2700K | 温馨氛围 |
| `睡前` | 2700K | 睡前放松 |
| `自然` | 4000K | 日常使用 |
| `冷白` | 5100K | 专注工作 |
| `阅读` | 5100K | 阅读学习 |

亮度参数可选，整数，范围 1–100。不传则保持当前亮度。

示例：
```
lamp 自然
lamp 自然 60
lamp 冷白 80
```

成功输出（不带亮度参数）：
```
场景：自然
```

成功输出（带亮度参数）：
```
场景：自然，亮度：60%
```

---

## 3. 退出码

| 退出码 | 含义 |
|--------|------|
| `0` | 命令执行成功 |
| `1` | 执行失败（参数错误、设备连接失败、环境配置缺失等） |

判断是否成功：检查退出码是否为 `0`。

---

## 4. 错误信息

所有错误信息输出到 **stderr**，成功结果输出到 **stdout**。

常见错误及原因：

| 错误信息 | 原因 | 处理建议 |
|----------|------|----------|
| `错误：.env 文件中缺少配置项：LAMP_IP, LAMP_TOKEN` | 环境变量未配置 | 检查项目目录下 .env 文件是否存在且包含正确配置 |
| `错误：未知命令 'xxx'` | 命令名拼写错误 | 检查命令是否在支持列表内（区分中英文、全半角） |
| `错误：请指定亮度值` | 亮度/色温命令缺少数值参数 | 补充数值参数 |
| `错误：参数无效 —— ...` | 参数不是合法整数 | 确保参数为纯数字 |
| `台灯控制失败：...` | 设备连接失败 | 检查台灯是否开机、是否在同一局域网、IP 是否正确 |

---

## 5. 使用限制

- **网络依赖**：台灯通过局域网 UDP 控制，执行机器必须与台灯（IP: `192.168.5.40`）在同一局域网
- **无状态**：每次调用独立执行，不维持连接，命令之间互不影响
- **并发安全**：多条命令同时发送可能导致设备状态不可预期，建议顺序执行
- **响应时间**：正常情况下单条命令在 2 秒内完成；超时则设备可能离线

---

## 6. 典型任务示例

**把灯调成适合阅读的状态：**
```
lamp 阅读
```

**把灯调暗一些准备睡觉：**
```
lamp 睡前 20
```

**检查台灯当前是否开着：**
```
lamp 状态
```
然后解析输出第一行：`状态：开` 表示开着，`状态：关` 表示关着。

**先开灯，再设置为自然光亮度 70%：**
```
lamp 开
lamp 自然 70
```
