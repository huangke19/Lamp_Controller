# 台灯控制程序 Agent 使用说明

本文档面向 AI 智能体（Agent）。描述如何在 macOS 终端中通过命令控制小米台灯2，包括调用方式、参数规则、输出格式、错误判断和使用限制。

---

## 1. 调用方式

### 首选方式

```
lamp <command> [args]
```

`lamp` 是 Go 编译的独立二进制文件，位于 `~/bin/lamp`。`~/bin` 已加入 `~/.zshenv`，**任何 shell 环境（包括非交互式）均可直接使用**，无需激活虚拟环境，无需切换目录。

### 备用方式（PATH 不可用时，使用完整路径）

```
/Users/huangke/bin/lamp <command> [args]
```

---

## 2. 支持的命令

### 2.1 开灯

```
lamp on
```

成功输出：
```
lamp turned on
```

---

### 2.2 关灯

```
lamp off
```

成功输出：
```
lamp turned off
```

---

### 2.3 查询状态

```
lamp status
```

成功输出（示例）：
```
Status: on
Brightness: 80%
Temp: 4000K
```

- `Status` 的值为 `on` 或 `off`
- `Brightness` 范围 1–100，单位 %
- `Temp` 范围 2700–5100，单位 K

---

### 2.4 设置亮度

```
lamp brightness <value>
```

- 参数：整数，范围 1–100
- 超出范围会被自动截断（1 以下取 1，100 以上取 100）

示例：
```
lamp brightness 80
```

成功输出（实际生效值）：
```
brightness set to 80%
```

---

### 2.5 设置色温

```
lamp temp <value>
```

- 参数：整数，范围 2700–5100（单位 K，数值越大越冷白）
- 超出范围会被自动截断

示例：
```
lamp temp 4000
```

成功输出（实际生效值）：
```
temp set to 4000K
```

---

### 2.6 场景模式

```
lamp <scene> [brightness]
```

可用场景名（共 5 种）：

| 场景名 | 色温 | 适用场合 |
|--------|------|----------|
| `warm` | 3000K | 温馨氛围 |
| `night` | 2700K | 睡前放松 |
| `neutral` | 4000K | 日常使用 |
| `cool` | 5000K | 专注工作 |
| `focus` | 4500K | 阅读学习 |

亮度参数可选，整数，范围 1–100。不传则保持当前亮度。

示例：
```
lamp neutral
lamp neutral 60
lamp cool 80
```

成功输出（不带亮度参数）：
```
Scene: neutral
```

成功输出（带亮度参数）：
```
Scene: neutral, brightness: 60%
```

---

## 3. 退出码

| 退出码 | 含义 |
|--------|------|
| `0` | 命令执行成功 |
| `1` | 执行失败（参数错误、设备连接失败、环境配置缺失等） |

判断是否成功：检查退出码是否为 `0`。

---

## 4. 错误信息

所有错误信息输出到 **stderr**，成功结果输出到 **stdout**。

常见错误及原因：

| 错误信息 | 原因 | 处理建议 |
|----------|------|----------|
| `错误：.env 文件中缺少配置项：LAMP_IP, LAMP_TOKEN` | 环境变量未配置 | 检查项目目录下 .env 文件是否存在且包含正确配置 |
| `错误：未知命令 'xxx'` | 命令名拼写错误 | 检查命令是否在支持列表内（区分中英文、全半角） |
| `错误：请指定亮度值` | 亮度/色温命令缺少数值参数 | 补充数值参数 |
| `错误：亮度必须是整数` | 参数不是合法整数 | 确保参数为纯数字 |
| `台灯控制失败：...` | 设备连接失败 | 检查台灯是否开机、是否在同一局域网、IP 是否正确 |

---

## 5. 使用限制

- **网络依赖**：台灯通过局域网 UDP 控制，执行机器必须与台灯在同一局域网
- **无状态**：每次调用独立执行，不维持连接，命令之间互不影响
- **并发安全**：多条命令同时发送可能导致设备状态不可预期，建议顺序执行
- **响应时间**：正常情况下单条命令在 2 秒内完成；超时则设备可能离线

---

## 6. 典型任务示例

**把灯调成适合阅读的状态：**
```
lamp focus
```

**把灯调暗一些准备睡觉：**
```
lamp night 20
```

**检查台灯当前是否开着：**
```
lamp status
```
然后解析输出第一行：`Status: on` 表示开着，`Status: off` 表示关着。

**先开灯，再设置为自然光亮度 70%：**
```
lamp on
lamp neutral 70
```
